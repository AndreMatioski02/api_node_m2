class Animal {
  constructor(
    id,
    name,
    birthDate,
    genre,
    breed,
    clientName
    
  ) {
    this.id = id
    this.name = name
    this.birthDate = birthDate
    this.genre = genre
    this.breed = breed
    this.clientName = clientName
  }
}

module.exports = Animal